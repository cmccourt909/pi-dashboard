const lines = [
'import { api } from "@/lib/api";',
'import { notFound } from "next/navigation";',
'',
'function healthToStatus(health) {',
'  if (health === "green") return "healthy";',
'  if (health === "red") return "critical";',
'  if (health === "yellow") return "warning";',
'  return "unknown";',
'}',
'',
'function statusColor(status) {',
'  if (status === "healthy") return "var(--status-healthy)";',
'  if (status === "critical") return "var(--status-critical)";',
'  if (status === "warning") return "var(--status-warning)";',
'  return "var(--text-muted)";',
'}',
'',
'function SectionLabel({ children }) {',
'  return (',
'    <p style={{ fontFamily: "var(--font-mono)", fontSize: 10, fontWeight: 700, letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--text-muted)", marginBottom: 4 }}>',
'      {children}',
'    </p>',
'  );',
'}',
'',
'function ProgressBar({ value, status }) {',
'  const color = statusColor(status);',
'  return (',
'    <div style={{ display: "flex", alignItems: "center", gap: 10 }}>',
'      <div style={{ flex: 1, height: 6, background: "var(--track-bg)", borderRadius: 2, overflow: "hidden", position: "relative" }}>',
'        <div style={{ position: "absolute", inset: 0, right: (100 - Math.min(100, value)) + "%", background: color, borderRadius: 2 }} />',
'      </div>',
'      <span style={{ fontFamily: "var(--font-mono)", fontSize: 11, color: color, minWidth: 36, textAlign: "right", fontWeight: 600 }}>',
'        {Math.round(value)}%',
'      </span>',
'    </div>',
'  );',
'}',
'',
'function SprintCard({ sprint }) {',
'  const stateColor = sprint.state === "active" ? "var(--status-warning)" : sprint.state === "closed" ? "var(--status-healthy)" : "var(--text-muted)";',
'  const status = sprint.state === "active" ? "warning" : sprint.state === "closed" ? "healthy" : "unknown";',
'  return (',
'    <div style={{ background: "var(--bg-card)", border: "1px solid var(--border)", borderLeft: "3px solid " + stateColor, borderRadius: 4, padding: "16px 20px" }}>',
'      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: 12 }}>',
'        <div>',
'          <p style={{ fontFamily: "var(--font-mono)", fontSize: 9, color: stateColor, fontWeight: 700, letterSpacing: "0.12em", textTransform: "uppercase", marginBottom: 4 }}>{sprint.state}</p>',
'          <h3 style={{ fontFamily: "var(--font-display)", fontSize: 16, fontWeight: 700, color: "var(--text-primary)" }}>{sprint.name}</h3>',
'        </div>',
'        {sprint.blocked_issues > 0 && (',
'          <span style={{ fontFamily: "var(--font-mono)", fontSize: 9, fontWeight: 700, color: "var(--status-critical)", border: "1px solid var(--status-critical)", borderRadius: 2, padding: "2px 6px", letterSpacing: "0.1em" }}>',
'            {sprint.blocked_issues} BLOCKED',
'          </span>',
'        )}',
'      </div>',
'      <div style={{ marginBottom: 12 }}>',
'        <ProgressBar value={sprint.pct_complete || 0} status={status} />',
'      </div>',
'      <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 8, paddingTop: 12, borderTop: "1px solid var(--border)" }}>',
'        {[',
'          { label: "Issues", value: sprint.total_issues },',
'          { label: "Done", value: sprint.done_issues },',
'          { label: "Blocked", value: sprint.blocked_issues },',
'        ].map(({ label, value }) => (',
'          <div key={label}>',
'            <SectionLabel>{label}</SectionLabel>',
'            <p style={{ fontFamily: "var(--font-mono)", fontSize: 16, fontWeight: 600, color: label === "Blocked" && value > 0 ? "var(--status-critical)" : label === "Done" && value > 0 ? "var(--status-healthy)" : "var(--text-primary)" }}>{value}</p>',
'          </div>',
'        ))}',
'      </div>',
'      {sprint.start_date && (',
'        <p style={{ fontFamily: "var(--font-mono)", fontSize: 9, color: "var(--text-muted)", marginTop: 12, letterSpacing: "0.05em" }}>',
'          {sprint.start_date} {"\u2192"} {sprint.end_date}',
'        </p>',
'      )}',
'    </div>',
'  );',
'}',
'',
'export default async function PIDetailPage({ params }) {',
'  const { name } = await params;',
'  const piName = decodeURIComponent(name);',
'  let pis = [];',
'  try {',
'    pis = await api.getPIs();',
'  } catch (e) {',
'    return (',
'      <div style={{ fontFamily: "var(--font-mono)", fontSize: 12, color: "var(--status-critical)", padding: 32 }}>',
'        Failed to load PI data. Make sure the backend is running on port 8000.',
'      </div>',
'    );',
'  }',
'  const pi = pis.find((p) => p.name === piName);',
'  if (!pi) notFound();',
'  const status = healthToStatus(pi.health);',
'  const color = statusColor(status);',
'  const activesprints = pi.sprints.filter((s) => s.state === "active");',
'  const closedsprints = pi.sprints.filter((s) => s.state === "closed");',
'  const futuresprints = pi.sprints.filter((s) => s.state === "future");',
'  return (',
'    <>',
'      <div style={{ marginBottom: 8 }}>',
'        <a href="/" style={{ fontFamily: "var(--font-mono)", fontSize: 10, color: "var(--text-muted)", letterSpacing: "0.1em", textDecoration: "none" }}>',
'          {"\u2190"} PI OVERVIEW',
'        </a>',
'      </div>',
'      <div style={{ marginBottom: 32, paddingBottom: 24, borderBottom: "1px solid var(--border)", display: "flex", justifyContent: "space-between", alignItems: "flex-end", flexWrap: "wrap", gap: 16 }}>',
'        <div>',
'          <p style={{ fontFamily: "var(--font-mono)", fontSize: 10, letterSpacing: "0.15em", color: "var(--text-muted)", marginBottom: 6 }}>PROGRAM INCREMENT</p>',
'          <h1 style={{ fontFamily: "var(--font-display)", fontSize: 36, fontWeight: 700, color: "var(--text-primary)", lineHeight: 1, marginBottom: 12 }}>PI {pi.name}</h1>',
'          <ProgressBar value={pi.pct_complete || 0} status={status} />',
'        </div>',
'        <div style={{ display: "flex", gap: 32, background: "var(--bg-panel)", border: "1px solid var(--border)", borderRadius: 4, padding: "16px 28px" }}>',
'          {[',
'            { label: "Sprints", value: pi.sprints.length },',
'            { label: "Issues", value: pi.total_issues },',
'            { label: "Done", value: pi.done_issues, accent: pi.done_issues > 0 ? "var(--status-healthy)" : undefined },',
'            { label: "Blocked", value: pi.blocked_issues, accent: pi.blocked_issues > 0 ? "var(--status-critical)" : undefined },',
'            { label: "Critical", value: pi.critical_findings, accent: pi.critical_findings > 0 ? "var(--status-critical)" : undefined },',
'            { label: "Complete", value: Math.round(pi.pct_complete || 0) + "%", accent: color },',
'          ].map(({ label, value, accent }) => (',
'            <div key={label}>',
'              <SectionLabel>{label}</SectionLabel>',
'              <p style={{ fontFamily: "var(--font-display)", fontSize: 22, fontWeight: 700, color: accent ?? "var(--text-primary)", lineHeight: 1 }}>{value}</p>',
'            </div>',
'          ))}',
'        </div>',
'      </div>',
'      {pi.sprints.length === 0 && (',
'        <div style={{ fontFamily: "var(--font-mono)", fontSize: 12, color: "var(--text-muted)", padding: "48px 0", textAlign: "center", letterSpacing: "0.1em" }}>',
'          NO SPRINTS PLANNED FOR THIS PI',
'        </div>',
'      )}',
'      {activesprints.length > 0 && (',
'        <div style={{ marginBottom: 32 }}>',
'          <p style={{ fontFamily: "var(--font-mono)", fontSize: 10, fontWeight: 700, letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--status-warning)", marginBottom: 12 }}>',
'            Active Sprints {"\u2014"} {activesprints.length}',
'          </p>',
'          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(280px, 1fr))", gap: 16 }}>',
'            {activesprints.map((s) => <SprintCard key={s.jira_id} sprint={s} />)}',
'          </div>',
'        </div>',
'      )}',
'      {futuresprints.length > 0 && (',
'        <div style={{ marginBottom: 32 }}>',
'          <p style={{ fontFamily: "var(--font-mono)", fontSize: 10, fontWeight: 700, letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--text-muted)", marginBottom: 12 }}>',
'            Upcoming Sprints {"\u2014"} {futuresprints.length}',
'          </p>',
'          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(280px, 1fr))", gap: 16 }}>',
'            {futuresprints.map((s) => <SprintCard key={s.jira_id} sprint={s} />)}',
'          </div>',
'        </div>',
'      )}',
'      {closedsprints.length > 0 && (',
'        <div style={{ marginBottom: 32 }}>',
'          <p style={{ fontFamily: "var(--font-mono)", fontSize: 10, fontWeight: 700, letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--text-muted)", marginBottom: 12 }}>',
'            Closed Sprints {"\u2014"} {closedsprints.length}',
'          </p>',
'          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(280px, 1fr))", gap: 16 }}>',
'            {closedsprints.map((s) => <SprintCard key={s.jira_id} sprint={s} />)}',
'          </div>',
'        </div>',
'      )}',
'    </>',
'  );',
'}',
];

require('fs').mkdirSync('app/pi/[name]', { recursive: true });
require('fs').writeFileSync('app/pi/[name]/page.tsx', lines.join('\n'));
console.log('PI detail page done');